from setuptools import setup

# Metadata goes in setup.cfg. These are here for GitHub's dependency graph.
setup(
    name="sweetrpg-shelf-objects",
    install_requires=[
        "marshmallow>=3,<5",
    ],
    extras_require={},
)
